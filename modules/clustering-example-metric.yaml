name: Clustering example metric
description: >
  Reference metric module from the omnibenchmark example benchmark. Scores a
  cluster assignment against the dataset labels and writes a one-line results
  table.
repository:
  url: https://github.com/omnibenchmark-example/metric.git
  commit: 579c643
stage: metrics
category: biofx
benchmarks: [clustering-example]
tags: [clustering, evaluation, example]
authors:
  - name: omnibenchmark
    github: omnibenchmark
license: MIT
inputs: [methods.mapping, data.meta]
outputs: [metrics.mapping]
